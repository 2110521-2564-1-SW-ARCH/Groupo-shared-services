message ChatMessage {
  required string messageID = 1;
  required string email = 2;
  required string message = 3;

  enum EventType {NEW = 0; DELETE = 1;}

  required EventType eventType = 4;
}

service ChatService {
  rpc PushMessage (ChatMessage) returns  {};
}
